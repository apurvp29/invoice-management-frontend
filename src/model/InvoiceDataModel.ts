export type InvoiceDataModel = {
  invoiceId: string;
  invoiceNumber: string;
  invoiceDate: string;
  dueDate: string;
  billedTo: string;
  billedBy: string;
  discount: number;
  createdAt: string;
  updatedAt: string;
};
